package com.lomekwi.cave.timeline;

import com.lomekwi.cave.ui.editpanel.tlarea.SegActor;

/** 测试用片段 Actor 桩：不创建任何 UI/资源，仅供 Segment 构造使用。 */
public class TestSegActor extends SegActor {
    public TestSegActor(Segment segment) {
        super(segment);
    }
}
